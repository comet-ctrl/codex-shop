# Luma Market

A single-page restaurant shop for Luma Market in Las Vegas. The site presents weekly prepared meals, fresh pasta, pantry goods, pickup ordering, restaurant reservations, and contact details.

## Run locally

Requires Node.js 22.13 or newer.

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
